# !!!РЕКОМЕНДУЕМЫЕ General СТРАТЕГИИ НА ДАННЫЙ МОМЕНТ!!!

**Рекомендуемые:**

* **General (4pda)**
* **General (10-4pda)**

---

## ℹ️ Краткое описание

Этот репозиторий содержит набор **стратегий (скриптов .bat)** и инструментов для обхода блокировок/ограничений (zapret). Важно: результат зависит от множества факторов — пробуйте разные стратегии (ALT, FAKE и другие), пока не найдёте рабочую для себя.

---

## 📁 Основные файлы

* [**`general.bat`**](./general.bat) — запуск стратегии вручную. Используется для проверки работоспособности.
* [**`service.bat`**](./service.bat) — управляющий скрипт с функциями автозапуска, диагностики и обновления.

---

## 🛠 `service.bat` — что умеет

* **`Install Service`** — установка выбранной стратегии в автозапуск (services.msc).
* **`Remove Services`** — удаление стратегии и WinDivert из служб.
* **`Check Status`** — проверка статуса обхода и служб (стратегии в автозапуске и WinDivert).
* **`Run Diagnostics`** — диагностика на распространённые причины, по которым zapret может не работать. В конце диагностики можно очистить кэш Discord, что иногда помогает.
* **`Check Updates`** — проверка обновлений репозитория.
* **`Switch Game Filter`** — переключение режима обхода для игр (UDP/TCP порты >1023). *После переключения требуется перезапуск стратегии.* Текущее состояние показывается в скобках (включено/выключено).
* **`Switch ipset`** — переключение режима фильтрации по `ipset-all.txt`. Возможные статусы:

  * `none` — никакие IP не попадают под проверку
  * `loaded` — IP проверяются по списку
  * `any` — любой IP попадает под фильтр
* **`Update ipset list`** — обновление `ipset-all.txt` из репозитория.

---

## ☑️ Частые вопросы и решения

### После запуска general* ничего не происходит

При запуске стратегии (отдельным `.bat`, не через `service`) должен открытьcя `winws.exe` (обход) — его видно в панели задач. Если этого не произошло — см. issue #522 (в репозитории).

### Обход не работает / перестал работать

* Стратегии со временем могут переставать работать — отдельная стратегия может быть обнаружена и стать неэффективной.
* В репозитории представлены разные стратегии; если ни одна не помогает — создайте новую на базе одной из существующих, изменив параметры.
* Проверьте `service.bat -> Run Diagnostics` на наличие ошибок.
* Убедитесь, что адрес ресурса добавлен в списки доменов или IP.
* Проверьте другие стратегии (ALT / FAKE и т. п.).
* Попробуйте полную переустановку (см. раздел ниже).

---

## 🔁 Полная переустановка / обновление

1. Сохраните ресурсы/данные, которые вы добавляли вручную.
2. Перезапустите устройство.
3. `service.bat -> Remove Services`.
4. `service.bat -> Run Diagnostics` (если есть ошибки — устраните их); в конце подтвердите `Y`.
5. Удалите папку с проектом.
6. Скачайте последнюю версию со страницы релизов (zapret-...)
7. Распакуйте в новую папку в корне диска (без спецсимволов и пробелов в пути).
8. Пробуйте запускать разные `general` скрипты — если какой-то не работает, закройте программу (в панели задач иконка замочка) и пробуйте другую стратегию.
9. Когда найдёте рабочую стратегию — поставьте её на автозапуск: `service.bat -> Install Service` → выберите нужную.

---

## 🎮 Проблемы с играми/приложениями

Если игра/приложение не работает с включённым обходом — проверьте, чтобы в `service.bat` были установлены `Game Filter disabled` и `ipset empty`. Иначе фильтр может затронуть неожиданные ресурсы.

---

## 🛡 Античит и WinDivert

Если античит жалуется на WinDivert — прочитайте инструкцию в соответствующем проекте (например, `zapret-win-bundle/windivert-hide`).

### Для Windows 7

Требуется цифровая подпись драйвера WinDivert: замените `WinDivert.dll` и `WinDivert64.sys` в папке `bin` на версии из `zapret-win-bundle/win7`.

---

## 🧹 WinDivert остаётся в службах после удаления

1. Узнайте имя службы: откройте cmd и выполните

```bash
driverquery | find "Divert"
```

2. Остановите и удалите её:

```bash
sc stop название_из_первого_шага
sc delete название_из_первого_шага
```

---

## ➕ Добавление адресов для обхода

Файлы со списками для расширения адресов:

* `list-general.txt` — домены (поддомены учитываются автоматически)
* `list-exclude.txt` — исключения доменов (если IP сети указан в `ipset-all.txt`, но конкретный домен не нужно фильтровать)
* `ipset-all.txt` — IP и подсети
* `ipset-exclude.txt` — исключения IP и подсетей
